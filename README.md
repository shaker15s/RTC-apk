# مسار RTC الموحد (Masar RTC — Mobile & Universal Web) 📱💻✨

**تطبيق ومنظومة مراكز رسالة للتدريب (Resala Training Centers - RTC)**

تطبيق موحد 100% مبني بـ **React Native 0.81 و Expo SDK 54 مع TypeScript**، يعمل بكفاءة متطابقة وميزات كاملة على:
- 📱 **الهواتف الذكية (Android & iOS)** — Native App عبر Expo و EAS Build.
- 💻 **الويب والحواسب (Universal Web)** — تجربة كاملة متجاوبة بتصميم Mobile-App Frame فخم على المتصفح.

---

## 🌟 أبرز ما تم إنجازه وتحديثه في هذا الإصدار:

1. **دعم الويب الشامل (Universal Web Parity):**
   - تصميم Responsive فخم بإطار 480px مريح للعين مع ظلال عميقة على شاشات الكمبيوتر.
   - ماسح QR حقيقي يعمل عبر كاميرا المتصفح (`HTML5 Video / getUserMedia`) مع إمكانية الإدخال اليدوي.
   - دعم الوضع الليلي (Dark Mode) وتبديل اللغات (العربية RTL والإنجليزية LTR) فورياً على الويب والموبايل.
   - استقرار كامل للملاحة ومنع تكرار الـ Render (`Maximum call stack size`).
   - طباعة الشهادات وحفظها بجودة فائقة عبر `window.print()` ومشاركة الروابط عبر `Web Share API`.

2. **سجل حضور ذكي ومفصل للطالب (`StudentAttendanceScreen`):**
   - بطاقات عصرية لكل محاضرة توضح **اسم الكورس**، **عنوان المحاضرة**، **اليوم والتاريخ والوقت بالدقائق**.
   - إشارات ملونة واضحة لحالة الحضور (`حاضر ✓`، `متأخر ⏰`، `غائب ✕`، `عذر مقبول 📝`).
   - مؤشر الالتزام ونقاط الحضور المحصلة مع حاسبة الاستحقاق للشهادة (75%).

3. **حماية أمان وبداية سلسة:**
   - التوجيه الصارم للشاشات العامة دون فتح التحقق تلقائياً.
   - تكامل كامل مع 28 دالة Supabase PostgreSQL RPC المشفرة.

- **عمارة نيتف نقية 100%:** أداء سلس 60/120 FPS وتجربة استخدام أصلية بالكامل دون أي WebViews.
- **عقد الـ RPC الـ 26 كاملاً:** تكامل تام ومباشر مع قاعدة بيانات Supabase عبر استدعاءات PostgreSQL RPC الآمنة.
- **ماسح الـ QR الذكي بكاميرا الهاتف:** تسجيل حضور المحاضرات في ثانية واحدة عبر مسح كود QR حقيقي يظهر على شاشة المدرب.
- **تخزين مشفر وآمن (Keychain / Keystore):** حفظ بيانات الجلسة والمفاتيح عبر التشفير المعتمد للهاتف.
- **استجابة لمسية ذكية (Haptic Engine):** اهتزازات تفاعلية مخصصة عند الضغط والنجاح والتحذير.
- **تذكيرات وإشعارات المحاضرات المجدولة:** تسجيل جهازك للتنبيهات وتذكير قبل موعد المحاضرة عند الانضمام لمجموعة.
- **الوضع الليلي الاحترافي (OLED Dark Mode):** تصميم مريح للعين وموفر للبطارية من غير وميض عند الإقلاع.
- **حراس المسارات الأمنية (Route Guards):** عزل تام للصلاحيات بحسب دور المستخدم (طالب / متطوع / مشرف) على الشاشات والتبويبات.
- **وضع الأوفلاين الحقيقي:** كشف فعلي للشبكة عبر NetInfo مع شريط تنبيه وكاش للفروع والدورات.
- **سجل حضور تفصيلي للطالب:** كل محاضرة بحالتها (حاضر/متأخر/غائب/معذور) ونقاطها وتاريخها.
- **شهادات قابلة للمشاركة كصورة:** بطاقة شهادة معتمدة بتصميم رسمي + QR موثّق تُصدَّر كصورة PNG وتُشارك مباشرة.
- **تنقل نيتف حقيقي (React Navigation):** انتقالات أصلية، رجوع بالسحب على iOS، وروابط عميقة مثل `org.resala.rtc.masar://verify?serial=X`.
- **دعم ثنائي اللغة (العربية/English) كامل:** تبديل فوري من شاشة الحساب يغطي كل الشاشات (طالب/متطوع/مشرف) والمكونات عبر محرك i18n تفاعلي (719 مفتاحاً) مع سقوط تلقائي للعربية.
- **سجل حضور ذكي:** نسبة التزام حقيقية لكل دورة، أهلية الشهادة (75%) مع رسالة «تحتاج N محاضرات إضافية»، فلاتر بالحالة، وإجمالي نقاط الحضور.
- **شهادة PDF رسمية:** تصدير الشهادة كملف PDF (صورة الشهادة + السيريال + رابط التحقق) ومشاركتها مباشرة.

---

## 📂 هيكلية المشروع (Project Architecture)

```
rtc_mobile/
├── src/
│   ├── core/                    # النواة: الثوابت، الثيم، الترجمة، التخزين المشفر، الهابتيكس
│   │   ├── config/              # إعدادات ونقاط نهاية Supabase
│   │   ├── theme/               # منظومة ألوان مسار (OLED Dark & Light) ونسب التباين
│   │   ├── i18n/                # نصوص اللغتين العربية (RTL) والإنجليزية
│   │   ├── storage/             # التخزين المشفر عبر SecureStore
│   │   ├── security/            # دوال التعقيم، والتحقق من الأسماء، وأرقام الهواتف المصرية
│   │   └── native/              # الهابتيكس، الإشعارات المجدولة، والمشاركة
│   ├── data/                    # طبقة البيانات
│   │   ├── supabaseClient.ts    # عميل Supabase مهيأ للتخزين المشفر
│   │   ├── rpc/                 # الـ 26 استدعاء RPC موثقة ومحددة الأنواع
│   │   └── repositories/        # مستودعات الاستعلام، ورفع الملفات، والكاش
│   ├── state/                   # الحالة العامة
│   │   ├── authStore.ts         # إدارة الجلسة وتسجيل الدخول بحساب Google (PKCE)
│   │   └── appStore.ts          # الثيم، واللغة، ومراقبة حالة الشبكة، والتنبيهات
│   ├── components/              # المكونات المشتركة
│   │   ├── common/              # الأزرار، الكروت، حقول الإدخال، الشرائح، المفاتيح
│   │   ├── layout/              # الهيدر الزجاجي، شريط مراقبة الشبكة، وشريط التبويب السفلي
│   │   ├── feedback/            # التوست، المودال، ونوافذ الانتظار
│   │   └── qr/                  # مودال الكاميرا لمسح كود الـ QR
│   ├── screens/                 # الشاشات الـ 30 موزعة بحسب الصلاحيات
│   │   ├── public/              # البداية (Splash)، الدخول والتسجيل (Onboarding)، والتحقق
│   │   ├── student/             # لوحة الطالب، المقررات، النقاط، السجل، الشهادات، الأعذار، المتصدرين
│   │   ├── volunteer/           # لوحة المدرب، كشوف الطلاب، تسجيل الحضور، ومراجعة الأعذار
│   │   └── admin/               # لوحة المشرف، المستخدمين، الدورات، الشهادات، الفروع، واللجان
│   └── navigation/              # الملاح الرئيسي، إدارة السجل، وحراس المسارات
├── tests/                       # جناح الاختبارات الآلية ومطابقة العقود
├── docs/                        # وثائق البنية المعمارية ومصفوفة التطابق الكامل
├── app.json                     # إعدادات حزمة Expo و Android Package و iOS Bundle Identifier
├── package.json                 # الحزم والاعتماديات
└── tsconfig.json                # إعدادات TypeScript
```

---

## 🚀 التشغيل والتطوير المحلي (Local Development)

### 1. المتطلبات الأساسية
- تثبيت [Node.js](https://nodejs.org/) (إصدار 18 أو أحدث).
- تثبيت تطبيق **Expo Go** على هاتفك (أندرويد من Google Play أو iOS من App Store).

### 2. تثبيت الحزم
```bash
npm install
```

### 3. تشغيل خادم التطوير
```bash
npm start
# أو للتشغيل مع مسح الكاش:
npm run start:clear
```
- امسح رمز الـ QR الظاهر في التيرمينال عبر تطبيق **Expo Go** على هاتفك لتشغيل التطبيق فوراً مع التحديث المباشر (Hot Reloading).

---

## 🧪 تشغيل جناح الاختبارات الآلية (Test Suite)

قم بتشغيل الأمر التالي لاختبار مطابقة الـ 28 RPC والشاشات وحراس المسارات — الجناح يشمل فحص `tsc --noEmit` حقيقي وينفّذ دوال الأمان الفعلية من `src/core/security/sanitizers.ts`:
```bash
node tests/run-tests.js
```

---

## 📦 البناء والإنتاج وإصدار الحزم (Production Build & EAS)

معرف التطبيق الموحد: `org.resala.rtc.masar`

### 1. إعداد EAS CLI
```bash
npm install -g eas-cli
eas login
```

### 2. بناء ملف APK للتجربة المباشرة على أندرويد
```bash
eas build --platform android --profile preview
```

### 3. بناء ملف AAB لمتجر Google Play
```bash
eas build --platform android --profile production
```

### 4. بناء ملف IPA لمتجر Apple App Store
```bash
eas build --platform ios --profile production
```

---

## 🔄 آلية التحديث (Expo Go vs APK + OTA)

التحديث بيوصل بطريقتين مختلفتين تماماً حسب طريقة تشغيلك للتطبيق:

| الوضع | إزاي بيوصل التحديث |
|---|---|
| **Expo Go** (من بلاي ستور) | بس عبر السيرفر المحلي `npx expo start` — التعديلات بتوصل فورياً (Fast Refresh). **`eas update` مش بيأثر على Expo Go إطلاقاً.** |
| **APK حقيقي** (من `eas build`) | تحديثات OTA من فرع `preview` (القناة متظبطة في eas.json). التحديث بيتنزّل في الخلفية عند فتح التطبيق، ويتفعّل عند إعادة الفتح. |

> ⚠️ **سبب إن التحديث مكانش بيوصل:** ملف CI (`.github/workflows/ci.yml`) لسه شايل `working-directory: ./rtc_mobile` والمجلد ده مش موجود. غيّر المسار لجذر المشروع (احذف السطر) عشان `eas update` يشتغل بعد الدمج على `main`.

**القواعد الذهبية:**
1. تعديل على شاشات/منطق (JS/TSX) → `npx eas-cli update --branch preview --message "..."` كفاية.
2. تعديل على `app.json` أو أي حاجة نيتف → مطلوب `eas build` جديد.
3. أي **رفع للإصدار** (`version`) → مطلوب build جديد مرة واحدة، لأن الـ OTA مربوط بالإصدار (`runtimeVersion: appVersion`) — النسخ القديمة مش هتقبل تحديثات الإصدار الجديد.

---

## 🔒 حماية البيانات والأمان (Security Standards)

- **Zero-Trust Backend:** لا يمكن لأي مستخدم تجاوز الصلاحيات الممنوحة له بفضل RLS و `SECURITY DEFINER` في PostgreSQL.
- **Privacy Masking:** حجب الأرقام والأسماء الحساسة في السجلات العامة والشهادات الموثقة.
- **Hardware Encryption:** حفظ توكنات الجلسة داخل رقاقة الأمان بالهاتف (Hardware Security Module).

---
**جمعية رسالة للأعمال الخيرية — مراكز رسالة للتدريب (RTC)**  
*بكل فخر لخدمة وتدريب شباب مصر مجاناً بالكامل.*
