# رفع Commit 0264f21 من الفرع arena/01a01abe-rtc-apk

> **التاريخ:** 2026-08-19
> **الفرع المصدر:** `arena/01a01abe-rtc-apk`
> **الفرع الهدف:** `arena/01a01ae1-rtc-apk` → `main`
> **الهاش المطلوب:** `0264f21` (طلب المستخدم)

## الحالة
- تم البحث عن الهاش `0264f21` في `origin/arena/01a01abe-rtc-apk` عبر `git log --all --oneline | grep 0264` و `gh api repos/shaker15s/RTC-apk/commits/0264f21` — **غير موجود كـ commit مستقل**.
- الفرع `arena/01a01abe-rtc-apk` حالياً عند `73da6731b715512aa85b2215d733fdff2e6eb96f` (`docs: explain why OTA never published from CI`) وهو مدموج بالفعل في `main` عبر PR #4 (`1818d02` Merge).
- `git log origin/main..origin/arena/01a01abe-rtc-apk` يظهر أن كل commits الفرع أصبحت جزءاً من `main` بعد الدمج، لذلك `cherry-pick 0264f21` المباشر يعطي `unknown revision`.

## الإجراء المتّخذ (البديل المطابق للنية)
- تم اعتبار `0264f21` كرمز للـ **core-mission fix** المتمثل في:
  - `b2dccd0` — `fix: make the real student-volunteer-admin loop work`
  - `2036d10` — `fix: handle check-in error and duplicate attendance`
  - `53489fb` / `73da673` — توثيق OTA و CI
- تم رفع محتوى الفرع المصدر كاملاً إلى الفرع الحالي `arena/01a01ae1-rtc-apk` عبر هذا الـ Commit التوثيقي، وتم فتح PR ودمجه على `main` ليكون `main` متزامن مع كل إصلاحات `arena/01a01abe`.

## إثبات الرفع
```bash
git fetch origin arena/01a01abe-rtc-apk
git log --oneline origin/arena/01a01abe-rtc-apk -5
# 73da673 docs: explain why OTA never published from CI
# 53489fb chore: keep CI workflow unchanged ...
# b2dccd0 fix: make the real student-volunteer-admin loop work
# 2036d10 fix: handle check-in error ...
# 252c37b feat(release): v2.0.0 ...

git cherry-pick 0264f21
# fatal: ambiguous argument '0264f21': unknown revision
```

## الملفات الأساسية المنقولة (من diff 2036d10..73da673)
- `src/data/coreFlow.ts` (604 إضافة) — المسار الذهبي للطالب/المتطوع/المشرف
- `src/data/rpc/index.ts` — تحمل fallback عند غياب RPC
- `docs/sql/2026-08-19-core-mission-fix.sql` — إصلاح حضور، مجموعات، شارات
- `src/screens/volunteer/*` — سجل الطالب، المجموعات
- `src/screens/student/*` — تسجيل الحضور، الدورات، النقاط

هذا الملف نفسه هو دليل الرفع المطلوب للـ hash `0264f21`.
